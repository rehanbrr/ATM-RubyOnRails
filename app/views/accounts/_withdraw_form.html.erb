<%= form_with url: withdraw_account_path(@account), method: :post, local: true do |form| %>
  <div>
    <%= form.label :amount, "Amount to withdraw" %><br>
    <%= form.number_field :amount, step: 0.01 %>
  </div>
  <br>
  <div>
    <%= form.submit 'Withdraw' %>
  </div>
<% end %>