<%= form_with url: deposit_account_path(@account), method: :post, local: true do |form| %>
  <div>
    <%= form.label :amount, "Amount to deposit" %><br>
    <%= form.number_field :amount, step: 0.01 %>
  </div>
  <br>
  <div>
    <%= form.submit 'Deposit' %>
  </div>
<% end %>